Read and follow the validation agent instructions at `ia_docs/06-agente-validador.md`.

Validate the feature spec at: `working/$ARGUMENTS/spec.md`

If no argument is provided, ask which feature to validate by listing the folders inside `working/` (excluding `_template`).
